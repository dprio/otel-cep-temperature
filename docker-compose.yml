services:
  app-cep-temp-orch:
    build:
      context: .
      dockerfile: Dockerfile.CepTempOrch
    container_name: cep-temp-orch
    restart: always
    ports:
      - 8080:8080 
    volumes:
      - ./config:/app/config
